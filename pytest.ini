[pytest]
markers =
    db: tests that require PostgreSQL test database
    yt_integration: calls actual yt api
    transcription: tests that require the transcription environment
    slow: tests that are slow (e.g. UMAP fit)

