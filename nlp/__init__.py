"""Shared NLP text-prep helpers: sentence-boundary trim, punctuation, coref.

Canonical claim-extraction prompts live in ``nlp.claim_extraction``.
Algorithm APIs live here for reuse outside claims. Posts-JSON CLI adapters stay
in ``apps.claims.prepare``.
"""
